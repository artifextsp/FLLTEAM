// =====================================================================
//  Plantilla de configuración
//  1) Copia este archivo como `config.js` en la raíz del proyecto.
//  2) Reemplaza los valores con los de tu proyecto Supabase.
//  3) `config.js` está en .gitignore, no lo subas al repositorio.
// =====================================================================

window.FLL_CONFIG = {
    // URL pública del proyecto Supabase (Settings → API)
    SUPABASE_URL: "https://TU-PROYECTO.supabase.co",

    // Clave publicable (anon) — segura para el frontend
    SUPABASE_ANON_KEY: "TU_ANON_KEY_AQUI",
};
